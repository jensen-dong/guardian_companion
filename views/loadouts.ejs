<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Loadouts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">WeaponApp</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/api/destiny">Home</a>
                    </li>
                    <% if (currentUser) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/destiny/favorites">Favorites</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/api/destiny/loadouts">Loadouts</a>
                        </li>
                        <% } %>
                </ul>
                <ul class="navbar-nav ms-auto">
                    <% if (!currentUser) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/signup">Sign Up</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/auth/login">Sign In</a>
                        </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="/auth/logout">Logout</a>
                            </li>
                            <% } %>
                </ul>
            </div>
        </div>
    </nav>
        <div class="container mt-4">
            <h1 class="text-white">Loadouts</h1>
            <div class="row">
                <% loadouts.forEach((loadout, index)=> { %>
                    <div class="col-md-4 mb-4">
                        <div class="card h-100 bg-dark text-white">
                            <div class="card-body">
                                <h4 class="card-title">
                                    <%= loadout.name %>
                                </h4>
                                <div class="mb-2">
                                    <strong>Kinetic:</strong>
                                    <img src="https://www.bungie.net<%= loadout.gear.kinetic.icon %>"
                                        class="weapon-icon" alt="<%= loadout.gear.kinetic.name %>">
                                    <%= loadout.gear.kinetic.name %>
                                </div>
                                <div class="mb-2">
                                    <strong>Energy:</strong>
                                    <img src="https://www.bungie.net<%= loadout.gear.energy.icon %>" class="weapon-icon"
                                        alt="<%= loadout.gear.energy.name %>">
                                    <%= loadout.gear.energy.name %>
                                </div>
                                <div class="mb-2">
                                    <strong>Heavy:</strong>
                                    <img src="https://www.bungie.net<%= loadout.gear.heavy.icon %>" class="weapon-icon"
                                        alt="<%= loadout.gear.heavy.name %>">
                                    <%= loadout.gear.heavy.name %>
                                </div>
                                <button class="btn btn-sm btn-danger"
                                    onclick="removeLoadout('<%= loadout._id %>')">Delete</button>
                            </div>
                        </div>
                    </div>
                    <% if ((index + 1) % 3===0) { %>
            </div>
            <div class="row">
                <% } %>
                    <% }); %>
            </div>
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
        <script src="/scripts/script.js"></script>
</body>

</html>